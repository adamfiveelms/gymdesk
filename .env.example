# Application
SECRET_KEY=replace-with-a-long-random-secret
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Local DB default (SQLite) if DATABASE_URL is omitted
# DATABASE_URL=sqlite:///./adamdesk.db

# Production DB example (PostgreSQL)
DATABASE_URL=postgresql+psycopg://adamdesk:change-me@db:5432/adamdesk
